{
  "name": "lootbox",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ aodhgan/lootbox",
    "prepare:local": "mustache networks/local.json subgraph.template.yaml subgraph.yaml",
    "create-local": "graph create --node http://localhost:8020/ aodhgan/lootbox",
    "remove-local": "graph remove --node http://localhost:8020/ aodhgan/lootbox",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 aodhgan/lootbox"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0",
    "mustache": "^4.0.1"
  },
  "devDependencies": {
  }
}
